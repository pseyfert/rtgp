# obvious things which need fixing

[ ] check if sumw2 is set and enable error bars accordingly
[ ] go through line styles, marker styles, etc
[ ] revisit usage of gnuplot-iostream interface (tempfile instead?)

